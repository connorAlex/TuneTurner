import './authDetails.js'
import { credentials } from './authDetails.js';

function Login() {

  const client_id = credentials.client_id;
  const authEndpoint = "https://accounts.spotify.com/authorize"

  return (
    <div className="Login">
     
    </div>
  );
}

export default Login;
